import { TranslocoGlobalConfig } from '@jsverse/transloco-utils';

const config: TranslocoGlobalConfig = {
  rootTranslationsPath: 'apps/web-app/public/i18n/',
  langs: ['en', 'es', 'pt-BR'],
  keysManager: {},
};

export default config;
